// @ts-nocheck
/* eslint-disable */
/* tslint:disable */
/* prettier-ignore-start */

/** @jsxRuntime classic */
/** @jsx createPlasmicElementProxy */
/** @jsxFrag React.Fragment */

// This class is auto-generated by Plasmic; please do not edit!
// Plasmic Project: ssXJzNt8MiEu8LBoGu8nv3
// Component: 7pGfee8N7aE

import * as React from 'react';

import * as p from '@plasmicapp/react-web';
import * as ph from '@plasmicapp/react-web/lib/host';

import {
  hasVariant,
  classNames,
  wrapWithClassName,
  createPlasmicElementProxy,
  makeFragment,
  MultiChoiceArg,
  SingleBooleanChoiceArg,
  SingleChoiceArg,
  pick,
  omit,
  useTrigger,
  StrictProps,
  deriveRenderOpts,
  ensureGlobalVariants,
} from '@plasmicapp/react-web';

import '@plasmicapp/react-web/lib/plasmic.css';

import projectcss from './plasmic_scratch_pad.module.css'; // plasmic-import: ssXJzNt8MiEu8LBoGu8nv3/projectcss
import sty from './PlasmicAltButton.module.css'; // plasmic-import: 7pGfee8N7aE/css

export type PlasmicAltButton__VariantMembers = {};
export type PlasmicAltButton__VariantsArgs = {};
type VariantPropType = keyof PlasmicAltButton__VariantsArgs;
export const PlasmicAltButton__VariantProps = new Array<VariantPropType>();

export type PlasmicAltButton__ArgsType = {};
type ArgPropType = keyof PlasmicAltButton__ArgsType;
export const PlasmicAltButton__ArgProps = new Array<ArgPropType>();

export type PlasmicAltButton__OverridesType = {
  root?: p.Flex<'button'>;
  green?: p.Flex<'div'>;
  grey?: p.Flex<'div'>;
  buttonLabel?: p.Flex<'div'>;
};

export interface DefaultAltButtonProps {
  className?: string;
}

const __wrapUserFunction =
  globalThis.__PlasmicWrapUserFunction ?? ((loc, fn) => fn());
const __wrapUserPromise =
  globalThis.__PlasmicWrapUserPromise ??
  (async (loc, promise) => {
    return await promise;
  });

function PlasmicAltButton__RenderFunc(props: {
  variants: PlasmicAltButton__VariantsArgs;
  args: PlasmicAltButton__ArgsType;
  overrides: PlasmicAltButton__OverridesType;
  forNode?: string;
}) {
  const { variants, overrides, forNode } = props;

  const $ctx = ph.useDataEnv?.() || {};
  const args = React.useMemo(() => Object.assign({}, props.args), [props.args]);

  const $props = {
    ...args,
    ...variants,
  };
  const refsRef = React.useRef({});
  const $refs = refsRef.current;

  const currentUser = p.useCurrentUser?.() || {};
  const [$queries, setDollarQueries] = React.useState({});

  return (
    true ? (
      <p.Stack
        as={'button'}
        data-plasmic-name={'root'}
        data-plasmic-override={overrides.root}
        data-plasmic-root={true}
        data-plasmic-for-node={forNode}
        hasGap={true}
        className={classNames(
          projectcss.all,
          projectcss.button,
          projectcss.root_reset,
          projectcss.plasmic_default_styles,
          projectcss.plasmic_mixins,
          projectcss.plasmic_tokens,
          sty.root,
        )}
      >
        <div
          data-plasmic-name={'green'}
          data-plasmic-override={overrides.green}
          className={classNames(projectcss.all, sty.green)}
        >
          <div
            data-plasmic-name={'grey'}
            data-plasmic-override={overrides.grey}
            className={classNames(projectcss.all, sty.grey)}
          >
            <div
              data-plasmic-name={'buttonLabel'}
              data-plasmic-override={overrides.buttonLabel}
              className={classNames(
                projectcss.all,
                projectcss.__wab_text,
                sty.buttonLabel,
              )}
            >
              {'OK, let’s go!'}
            </div>
          </div>
        </div>
      </p.Stack>
    ) : null
  ) as React.ReactElement | null;
}

const PlasmicDescendants = {
  root: ['root', 'green', 'grey', 'buttonLabel'],
  green: ['green', 'grey', 'buttonLabel'],
  grey: ['grey', 'buttonLabel'],
  buttonLabel: ['buttonLabel'],
} as const;
type NodeNameType = keyof typeof PlasmicDescendants;
type DescendantsType<T extends NodeNameType> =
  (typeof PlasmicDescendants)[T][number];
type NodeDefaultElementType = {
  root: 'button';
  green: 'div';
  grey: 'div';
  buttonLabel: 'div';
};

type ReservedPropsType = 'variants' | 'args' | 'overrides';
type NodeOverridesType<T extends NodeNameType> = Pick<
  PlasmicAltButton__OverridesType,
  DescendantsType<T>
>;
type NodeComponentProps<T extends NodeNameType> =
  // Explicitly specify variants, args, and overrides as objects
  {
    variants?: PlasmicAltButton__VariantsArgs;
    args?: PlasmicAltButton__ArgsType;
    overrides?: NodeOverridesType<T>;
  } & Omit<PlasmicAltButton__VariantsArgs, ReservedPropsType> & // Specify variants directly as props
    /* Specify args directly as props*/ Omit<
      PlasmicAltButton__ArgsType,
      ReservedPropsType
    > &
    /* Specify overrides for each element directly as props*/ Omit<
      NodeOverridesType<T>,
      ReservedPropsType | VariantPropType | ArgPropType
    > &
    /* Specify props for the root element*/ Omit<
      Partial<React.ComponentProps<NodeDefaultElementType[T]>>,
      ReservedPropsType | VariantPropType | ArgPropType | DescendantsType<T>
    >;

function makeNodeComponent<NodeName extends NodeNameType>(nodeName: NodeName) {
  type PropsType = NodeComponentProps<NodeName> & { key?: React.Key };
  const func = function <T extends PropsType>(
    props: T & StrictProps<T, PropsType>,
  ) {
    const { variants, args, overrides } = React.useMemo(
      () =>
        deriveRenderOpts(props, {
          name: nodeName,
          descendantNames: [...PlasmicDescendants[nodeName]],
          internalArgPropNames: PlasmicAltButton__ArgProps,
          internalVariantPropNames: PlasmicAltButton__VariantProps,
        }),
      [props, nodeName],
    );
    return PlasmicAltButton__RenderFunc({
      variants,
      args,
      overrides,
      forNode: nodeName,
    });
  };
  if (nodeName === 'root') {
    func.displayName = 'PlasmicAltButton';
  } else {
    func.displayName = `PlasmicAltButton.${nodeName}`;
  }
  return func;
}

export const PlasmicAltButton = Object.assign(
  // Top-level PlasmicAltButton renders the root element
  makeNodeComponent('root'),
  {
    // Helper components rendering sub-elements
    green: makeNodeComponent('green'),
    grey: makeNodeComponent('grey'),
    buttonLabel: makeNodeComponent('buttonLabel'),

    // Metadata about props expected for PlasmicAltButton
    internalVariantProps: PlasmicAltButton__VariantProps,
    internalArgProps: PlasmicAltButton__ArgProps,
  },
);

export default PlasmicAltButton;
/* prettier-ignore-end */
